services:
  - type: worker
    name: mbti-morrowind-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
      - key: ADMIN_ID
